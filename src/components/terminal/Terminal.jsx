import React from "react";

const Terminal = (props) => {
    return (
        <div>
            <div className="">Terminal display - displays current status (hello, entered cashed, processing, return cash, not enough cash, no more products, ...)</div>

            <div className="">Cash acceptor - enter cash amount</div>

            <div className="">Product selector - numbers to enter product id</div>

            <div className="">Cash return</div>
        </div>
    )
}

export default Terminal;